<Window x:Class="EllahColNum.Revit.UI.EditMarkDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Column Mark"
        Width="380" Height="240"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#1E1E2E"
        FocusManager.FocusedElement="{Binding ElementName=NewMarkBox}">

  <Window.Resources>
    <Style x:Key="LabelStyle" TargetType="TextBlock">
      <Setter Property="Foreground"        Value="#A6ADC8"/>
      <Setter Property="FontSize"          Value="11"/>
      <Setter Property="Margin"            Value="0,0,0,4"/>
      <Setter Property="FontWeight"        Value="SemiBold"/>
    </Style>
    <Style x:Key="ValueStyle" TargetType="TextBlock">
      <Setter Property="Foreground"        Value="#CDD6F4"/>
      <Setter Property="FontSize"          Value="14"/>
      <Setter Property="Margin"            Value="0,0,0,12"/>
    </Style>
    <Style x:Key="EditBoxStyle" TargetType="TextBox">
      <Setter Property="Background"        Value="#313244"/>
      <Setter Property="Foreground"        Value="#CDD6F4"/>
      <Setter Property="BorderBrush"       Value="#585B70"/>
      <Setter Property="BorderThickness"   Value="1"/>
      <Setter Property="Padding"           Value="8,6"/>
      <Setter Property="FontSize"          Value="14"/>
      <Setter Property="CaretBrush"        Value="#CDD6F4"/>
    </Style>
    <Style x:Key="BtnStyle" TargetType="Button">
      <Setter Property="Padding"           Value="14,8"/>
      <Setter Property="Background"        Value="#313244"/>
      <Setter Property="Foreground"        Value="#CDD6F4"/>
      <Setter Property="BorderBrush"       Value="#585B70"/>
      <Setter Property="BorderThickness"   Value="1"/>
      <Setter Property="FontSize"          Value="12"/>
      <Setter Property="Cursor"            Value="Hand"/>
    </Style>
    <Style x:Key="BtnPrimary" TargetType="Button" BasedOn="{StaticResource BtnStyle}">
      <Setter Property="Background"        Value="#7C3AED"/>
      <Setter Property="Foreground"        Value="White"/>
      <Setter Property="BorderBrush"       Value="#7C3AED"/>
      <Setter Property="FontWeight"        Value="SemiBold"/>
    </Style>
    <Style x:Key="BtnFinish"  TargetType="Button" BasedOn="{StaticResource BtnStyle}">
      <Setter Property="Background"        Value="#16A34A"/>
      <Setter Property="Foreground"        Value="White"/>
      <Setter Property="BorderBrush"       Value="#16A34A"/>
      <Setter Property="FontWeight"        Value="SemiBold"/>
    </Style>
  </Window.Resources>

  <Grid Margin="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="Current mark"
               Style="{StaticResource LabelStyle}"/>
    <TextBlock x:Name="CurrentMarkText" Grid.Row="1"
               Style="{StaticResource ValueStyle}"/>

    <TextBlock Grid.Row="2" Text="New mark"
               Style="{StaticResource LabelStyle}"/>
    <TextBox   x:Name="NewMarkBox" Grid.Row="3"
               Style="{StaticResource EditBoxStyle}"/>

    <Grid Grid.Row="5" Margin="0,16,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="8"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>

      <Button Grid.Column="0" Content="Cancel"
              Style="{StaticResource BtnStyle}"
              Click="CancelBtn_Click"
              IsCancel="True"/>
      <Button Grid.Column="2" Content="Save  →  Pick next"
              Style="{StaticResource BtnPrimary}"
              Click="SaveBtn_Click"
              IsDefault="True"/>
      <Button Grid.Column="4" Content="✓  Save and Done"
              Style="{StaticResource BtnFinish}"
              Click="SaveAndDoneBtn_Click"/>
    </Grid>
  </Grid>
</Window>
